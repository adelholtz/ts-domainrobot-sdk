{
    "name": "ts-domainrobot-sdk",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "prestart": "yarn build",
        "watch": "nodemon --watch src --watch index.ts --exec yarn start",
        "start": "node .",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "version": "1.0.0",
    "license": "MIT",
    "dependencies": {
        "express": "^4.17.1",
        "js-domainrobot-sdk": "^1.2.2",
        "nodemon": "^2.0.4"
    },
    "devDependencies": {
        "@adelholtz/eslint-config": "https://github.com/adelholtz/eslint-config.git",
        "@types/express": "^4.17.7",
        "@types/node": "^14.0.23",
        "typescript": "^3.9.6"
    }
}
